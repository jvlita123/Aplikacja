@model Data.Entities.Cycle
@{
    ViewData["Title"] = "GetCycle";
    var path = Model.SourcePath;
}

<div class="row" style="padding:15px">
    <div class="col-md-12">
        <div class="d-flex justify-content-between align-items-center">
            <h3>@Model.Title</h3>
            <p>@Model.StartDate - @Model.EndDate</p>
        </div>

        <div class="row">
            <div class="col-md-6">
                <p>@Model.Description</p>
                <p>@Model.SourcePath</p>
            </div>
        </div>
    </div>
</div>

<form method="post" enctype="multipart/form-data" asp-controller="Cycles" asp-action="UploadFile">
    <div class="mb-3">
        <input class="form-control" type="file" name="uploadFileCycle" id="uploadFileCycle" />
    </div>
    <input type="hidden" name="id" id="id" value="@Model.Id" />
    <button type="submit" class="btn btn-primary">
        <i class="mdi mdi-upload"></i> Send File
    </button>
</form>

<a href="~/@path" class="btn btn-success" download>
    <i class="mdi mdi-download"></i> Download File
</a>
